(function(key, server) {
    $.get(
        "http://" + server + "/visit",
        { key: key, title: document.title, url: document.URL },
        function(response) {
            response.apply(key, server);
        },
        "jsonp");
})('<%= @key %>', '<%= @server %>')
