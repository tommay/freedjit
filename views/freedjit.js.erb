(function(id, key) {
    $.get(
        "http://<%= @server %>/visit",
        { key: key, title: document.title, url: document.URL },
        function(data) {},
        "jsonp");
    var list = function() {
        $.get(
            "http://<%= @server %>/list",
            { key: key },
            function(data) {
                $(id).html(data);
            },
            "jsonp");
    };
    list();
    setInterval(list, 60000);
})('#list', '<%= @key %>')
